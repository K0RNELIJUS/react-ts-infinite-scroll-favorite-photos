export type PhotoProps = {
  id: string;
  title: string;
  ownername: string;
  farm: string;
  server: string;
  secret: string;
  url: {
    sm: string;
    md: string;
    lg: string;
  };
};
